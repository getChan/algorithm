from sys import stdin
read = stdin.readline

c = int(read().rstrip())

for _ in range(c):
    n, m = (int(x) for x in read().rstrip())

    a = [int(x) for x in read().rstrip()]
    b = [int(x) for x in read().rstrip()]

    
